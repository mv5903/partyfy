const TEST_BASE_URL = 'http://localhost:3000';

const USERNAME = 'test' + new Date().getTime().toString();
const PASSWORD = 'PartyfyTest123!';

export { 
    TEST_BASE_URL,
    USERNAME,
    PASSWORD
};